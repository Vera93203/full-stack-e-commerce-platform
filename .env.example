# Optional: Google Maps Places autocomplete at checkout
# https://developers.google.com/maps/documentation/places/web-service
GOOGLE_MAPS_PLATFORM_KEY=

# Production mode (serves built static assets instead of Vite dev middleware)
# NODE_ENV=production

# Reserved for future AI features (not used by the app currently)
# GEMINI_API_KEY=
